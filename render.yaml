services:
  - type: web
    name: dsn-mcp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: PORT
        value: 3000
      - key: API_KEY
        generateValue: true
